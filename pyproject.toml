[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "sqlch"
version = "0.1.0"
dependencies = [
    "requests",
    "textual",
]

[project.scripts]
sqlch = "cli.main:main"

[tool.setuptools]
packages = ["cli", "core", "tui"]
